

#include "Menu.h"

void Menu::updateFrame(float &elapsed) {
    State::updateFrame(elapsed);
}

void Menu::updateEntities(float &elapsed) {
    State::updateEntities(elapsed);
}

void Menu::processInputs(float &elapsed) {
    State::processInputs(elapsed);
}
